/**
 * UNIFIED PositionState: {x, y, sticky, locked, t0} per node.
 * @module layout/positions
 */

export function createPositionState(x, y) {}

export function updatePosition(state, nodeId, x, y) {}
