/**
 * Initial layout + streaming placement via manifold-interpolation.
 * @module layout/placement
 */

export function initialPlace(nodes, edges, W) {}

export function streamPlace(newNode, existingPositions) {}
