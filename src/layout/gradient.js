/**
 * Energy function E + gradient descent step.
 * @module layout/gradient
 */

export function energy(positions, edges, W, context) {}

export function gradEnergy(positions, edges, W, context) {}

export function descentStep(positions, edges, W, context, dt) {}
