/**
 * Cluster topology changes (independent of zoom); respects context pins.
 * @module navigation/expand-collapse
 */

export function expandCluster(clusterId, context) {}

export function collapseCluster(clusterId, context) {}
