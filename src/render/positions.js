/**
 * renderPositions() - called ONCE per frame by the scheduler.
 * Transforms all visible elements to their current positions.
 * @module render/positions
 */

export function renderPositions(state) {}
