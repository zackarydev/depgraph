/**
 * X-key: positions -> T0, weights -> context defaults (NOT time travel).
 * @module interact/reset
 */

export function startReset() {}

export function updateReset(elapsed) {}

export function stopReset() {}
