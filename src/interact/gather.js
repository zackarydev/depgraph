/**
 * Neighbor / cluster / intra-cluster / space-pull (one engine).
 * @module interact/gather
 */

export function startGather(nodeIds) {}

export function updateGather(elapsed) {}

export function stopGather() {}
