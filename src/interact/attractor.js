/**
 * Force-press / shift-hold pull: ramps attraction strength, BFS depth by hold duration.
 * @module interact/attractor
 */

export function startAttractor(nodeId) {}

export function updateAttractor(elapsed) {}

export function stopAttractor() {}
