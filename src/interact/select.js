/**
 * Single selectedNode + selectedNodes Set, one code path.
 * @module interact/select
 */

export function selectNode(bus, nodeId) {}

export function toggleSelection(bus, nodeId) {}

export function clearSelection(bus) {}
