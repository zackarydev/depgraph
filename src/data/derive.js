/**
 * Compute hyperedges / affinities / clusters from edges.
 * Re-derives on W or edge change; dirty-propagation.
 * @module data/derive
 */

export function deriveHyperEdges(edges) {}

export function deriveAffinities(node, edges, W) {}

export function deriveClusters(nodes, affinities) {}

export function invalidate(edgeId) {}

export function recompute() {}
