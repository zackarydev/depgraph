/**
 * Snapshot writing / loading; segment addressing.
 * @module data/snapshot
 */

export function writeSnapshot(state, cursor) {}

export function loadSnapshot(json) {}

export function loadWithTail(snapshotJson, tailCsv) {}
